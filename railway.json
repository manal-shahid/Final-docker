{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "install": "cd backend && npm install",
      "build": "cd backend && npm run build || echo 'no build step'",
      "start": "cd backend && node server.js"
    }
  }
}
